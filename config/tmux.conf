# Minimal Hatchery tmux configuration. The default Ctrl+b prefix is unchanged.
set -g mouse on
set -g history-limit 50000
set -g default-terminal "tmux-256color"
set -g status-left "[#S] "
set -g status-right "%Y-%m-%d %H:%M"
